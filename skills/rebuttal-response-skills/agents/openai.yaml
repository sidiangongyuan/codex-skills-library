interface:
  display_name: "Rebuttal Response Skills"
  short_description: "Draft direct, reviewer-mapped author responses"
  default_prompt: "Use $rebuttal-response-skills with the exact review text and submitted paper, preserve concern order, select the required PDF or OpenReview format, and use only verified evidence."

policy:
  allow_implicit_invocation: true
