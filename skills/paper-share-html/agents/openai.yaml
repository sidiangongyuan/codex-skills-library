interface:
  display_name: "Paper Share HTML"
  short_description: "Build clear, varied paper presentations"
  default_prompt: "Use $paper-share-html to build a source-grounded paper presentation with readable tables, varied explanatory visuals, and browser QA."

policy:
  allow_implicit_invocation: true
