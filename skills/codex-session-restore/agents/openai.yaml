interface:
  display_name: "Codex Session Restore"
  short_description: "Restore Codex Desktop sessions"
  default_prompt: "Use $codex-session-restore to restore active Codex sidebar sessions after a provider switch without changing current auth."

policy:
  allow_implicit_invocation: true
